let firstName = "Terry";
let lastName;

// console.log(`${firstName || "No first Name"} ${lastName}`);

let isActive = true;
const message = isActive ? "Está activo" : "No está activo"


console.log(message);